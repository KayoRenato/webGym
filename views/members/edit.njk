{% extends "layout.njk" %}

{% block main %}
  <div class="card">

      <section class="avatar" style= "background: url({{member.avatarURL}}) no-repeat center center / cover "></section>
      <section class="details">

        <form method="POST" action="/members?_method=PUT">
          <h3>Editar Aluno</h3>
          {% include "members/form.njk" %}
            <input
              type="hidden"
              name="id" 
              value={{member.id}}
            >
            <button type="submit">Salvar</button>
        </form>

        <form id="delete" action="/members?_method=DELETE" method="post">
          <input
                type="hidden"
                name="id" 
                value={{member.id}}
          >
          <button type="submit">Deletar</button>
        </form>
      </section>
  </div>

{% endblock %}